// embedding means text to numbers(vector)  vector dbs stores vector embeddings
// car booking app  -> [0.21, -0.82, 0.55, ...]
// real embeddings : 768 dimensions , 1536 dimesions, 3072 dimesions


// rag
// llm  + knowledge => RAG(retrieval augmented generation)
// so ai will not only use its training knowledge , it will now search in data , bring out relevant information and then generate answer

// pdf read -> stores -> fetch relevant question answers -> gives it to LLM -> LLM answers then4 ()

// popular embedding model => text-embedding-004 (google) => text-embedding-3-small (openAI)